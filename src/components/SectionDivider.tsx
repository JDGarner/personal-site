import styled from "styled-components";
import { highlightColorBlue } from "../styling/palette";

const SectionDivider = styled.div`
  height: 4px;
  width: 100%;
  background-color: ${highlightColorBlue};
`;

export default SectionDivider;
